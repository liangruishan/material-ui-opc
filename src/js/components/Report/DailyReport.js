'use strict';
let { Table, TableBody, TableFooter, TableHeader, TableRowColumn,TableHeaderColumn, TableRow } = require('material-ui');

let React = require('react');
let PropTypes = React.PropTypes;

var Report = React.createClass({
  propTypes:{
    reportable: PropTypes.arrayof(object).isRequired
  },

  render: function(){
    return(
      
    );
  }
});
